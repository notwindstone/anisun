/**
 * Dependencies for Remote Components (only for react)
 */
module.exports = {
    resolve: {
        react:              require("react"),
        "react-dom/client": require("react-dom/client"),
    },
};
